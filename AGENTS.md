# Master Ten 継続開発指示

## 設計資料と知的財産

- `docs/master-ten-research-and-spec.md` を本プロジェクトの主要な調査・設計資料として扱う。
- 調査レポートを削除、改名、内容の出典区分を曖昧化しない。
- Easybrain または類似ゲームのコード、問題配置、文章、画像、アイコン、音声、画面構成をコピーしない。
- 参考製品について、公式文章で確認した事実、公開画像からの観察、非公式情報、推測を区別し、推測を公式仕様として説明しない。
- Master Ten の名称、コード、問題、文章、配色、アイコン、効果音、情報設計は独自にする。

## 品質ゲート

- 各フェーズでテストとビルドを実行し、成功後に必要なファイルだけをコミットして `main` へ通常 push する。
- push 前に最低でも `npm test` と `npm run build` を実行する。
- ローカルコミット SHA と `origin/main` の SHA が一致すると確認できない状態を完了としない。
- `--force`、`--force-with-lease`、`reset --hard`、`git clean -fd` を使用しない。
- テストを無効化したり、失敗テストを削除するだけで品質ゲートを通さない。

## ルール・探索の不変条件

- `SOLVED`、`UNSOLVABLE`、`UNKNOWN` を明確に区別する。探索予算切れを `UNSOLVABLE` としない。
- ソルバー未検証または結果が `UNKNOWN` の問題を公開問題へ入れない。
- ヒントは検証済み完走経路上の安全な手だけを示す。安全性を証明できないときは手を提示しない。
- ルールエンジンは UI と分離し、入力を破壊しない決定論的な純粋遷移を維持する。
- 問題、ルール、生成器、難易度、保存形式は版を持ち、版の不一致を黙って受け入れない。

## 公開とプライバシー

- ゲーム中の外部通信、広告、分析、認証、個人情報収集を追加しない。
- GitHub Pages の `/master-ten-pwa/` サブパスとオフライン動作を維持する。
- 秘密情報、API キー、トークン、`node_modules` をコミットしない。
